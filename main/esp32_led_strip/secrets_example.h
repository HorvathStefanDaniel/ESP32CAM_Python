/*
 * WiFi credentials for LED strip controller.
 * Copy this to secrets.h and fill in, or copy secrets.h from the main folder.
 */

#ifndef SECRETS_H
#define SECRETS_H

const char* ssid     = "stefanIot";   // Same network as ESP32-CAM
const char* password = "YOUR_WIFI_PASSWORD";

#endif
